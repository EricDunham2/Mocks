<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?bgColor">
<LinearLayout
    android:id="@+id/activity_beam"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.projects.mocks.mocks.BeamActivity"
    android:orientation="vertical"
    android:background="?bgColor">

    <TextView
        android:text="To share your portfolio, just tap phones with another nfc-enabled phone!"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/textView"
        android:textAppearance="@android:style/TextAppearance.Material.Medium"
        android:textAlignment="center"
        android:padding="10dp"
        android:background="@color/colorPrimary"/>

    <TextView
        android:text="SoAndSoAndSo's Portfolio"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/tvPortfolioSummary"
        android:textAlignment="center"
        android:textColor="?txtColor"
        android:layout_marginTop="100dp"
        android:textSize="20sp"
        android:gravity="center"
        android:background="?attr/colorAccent"
        android:paddingTop="20dp"
        android:paddingBottom="20dp"/>

    <ListView
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:layout_weight="1"
        android:textColor="?txtColor"
        android:nestedScrollingEnabled="true"
        android:background="@drawable/shape_border"
        android:divider="?attr/listViewOutlineColor"
        android:id="@+id/lvSymbols"/>

    <Button
        android:text="Save To File"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/button3"
        android:layout_marginTop="50dp"
        style="@style/FillButton"
        android:backgroundTint="@color/colorBad"/>

</LinearLayout>
</ScrollView>